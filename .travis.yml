language: node_js
sudo: false
node_js:
  - v7
cache: yarn
install:
  - npm install -g codecov
  - npm install -g yarn
  - yarn install --ignore-optional
script:
  - npm run lint
  - npm run coverage
  - npm run build
