query ResourcesOverTime($id: String!) {
  internationalResources(id: $id) {
    startYear
    resourcesOverTime {
      data {
        year
        value
        flow_name
        flow_category
        flow_type
        direction
        color
      }
    }
  }
}
