// @flow
import React from 'react';
import Container from 'components/atoms/DarkTabsContainer';

const DataRevolution = (props: Object) => (
  <Container options={props.options}>
    <div />
  </Container>
);

export default DataRevolution;
